<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#4D9BD3"
    android:padding="16dp">

    <!-- Radio Group for Choosing Car or Bus Reservation List -->
    <RadioGroup
        android:id="@+id/radioGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp">

        <!-- Radio Button for Car Reservation -->
        <RadioButton
            android:id="@+id/radioCar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Car Reservation" />

        <!-- Radio Button for Bus Reservation -->
        <RadioButton
            android:id="@+id/radioBus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bus Reservation"
            android:layout_marginStart="16dp" />
    </RadioGroup>

    <!-- Clear All Button -->
    <Button
        android:id="@+id/clearAllButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Clear All" />

    <!-- RecyclerView for Displaying the Reservation List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp" />

    <!-- Back Button at the Bottom -->
    <Button
        android:id="@+id/btnBackToTransportManagement"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back to Transport Management"
        android:layout_gravity="center_horizontal" />
</LinearLayout>
